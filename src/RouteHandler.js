import React from "react";
import { 
    BrowserRouter,
    Routes,
    Route,
} from "react-router-dom";

const RouteHandler = () => {
    return (
        <BrowserRouter>
            <Routes>
            </Routes>
        </BrowserRouter>
    );
}

export default RouteHandler;